// 获取到更改前的值存储起来，然后进行更新

export default class Watcher {
    /**
     * @param {*} vm: 当前vue的实例 
     * @param {*} key: 当前存储的值
     * @param {*} callback: 更新视图
     */
    constructor(vm, key, callback) {

    }

    // 数据变化时，更新dom
    update() {
        
    }
}